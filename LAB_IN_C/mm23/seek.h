#ifndef SEEK_H
#define SEEK_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/* for a given char prints the ASCI CODE*/
int print(char c);
/*return num of chars in a given file*/
int num_of_chars(FILE * file);

#endif
